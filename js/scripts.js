// animate star //

var $V = $('#V');

//selector or DOM element, duration, CSS properties

TweenMax.from($V, 5, {
    
    opacity: 0,
    x: 333
    
 
    
});

var $C = $('#C');

//selector or DOM element, duration, CSS properties

TweenMax.from($C, 5, {
    
    opacity: 0,
    y: 333
    
 
    
});

var $D = $('#D');

//selector or DOM element, duration, CSS properties

TweenMax.from($D, 5, {
    
    opacity: 0,
    x: 333
    
 
    
});